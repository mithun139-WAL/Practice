// const { Pool } = require('pg');

// const pool = new Pool({
//   user: process.env.PG_USER || 'mithun',
//   host: process.env.PG_HOST || 'localhost',
//   database: process.env.PG_DATABASE || 'db1',
//   password: process.env.PG_PASSWORD || 'alpha1398',
//   port: process.env.PG_PORT || 5432,
// });

// module.exports = {
//   query: (text, params) => pool.query(text, params),
// };
